version: '2'

services:

  collectd:
    build:
      context: collectd/
    networks:
      - elk

  influxdb:
    build:
      context: influxdb/
    networks:
      - elk

  grafana:
    build:
      context: grafana/
    networks:
      - elk

networks:

  elk:
    driver: bridge
